<% provide(:title, 'Add News Article') %>
<h1>Add News Article</h1>
<%= form_for(@article) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.label :title %>
    <%= f.text_field :title, class: "input-large" %>
    
    <%= f.label :body %>
    <%= f.text_area :body %>
    
  <%= f.submit "Publish Article", class: "btn btn-large btn-primary" %>
<% end %>